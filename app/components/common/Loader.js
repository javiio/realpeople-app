import React from 'react';
import { ActivityIndicator, View } from 'react-native';

const Loader = () => {
  return (
    <View>
      <ActivityIndicator />
    </View>
  );
};

export default Loader;
